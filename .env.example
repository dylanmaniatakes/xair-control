# Optional. Copy to .env beside the Compose file.
WEB_PORT=8088
# All host interfaces for trusted-LAN access; use 127.0.0.1 for host-only access.
BIND_ADDRESS=0.0.0.0
# For compose.github.yaml: change main to an existing tag or full commit hash.
# XAIR_SOURCE=https://github.com/DylanManiatakes/xair-control.git#main
# Configure your mixer in the web UI. No mixer address belongs in this file.
